// A Plataforma - Login Controller
//
// Angular.JS Controller to handle login, signup and passord reset.
//

app.controller('loginController', ['$scope', '$loginService', function($scope, $loginService){

	/**
		$scope.users = [];

		var Refresh = function() {
			$loginService.login();
			$scope.user = '';
		};

	    $scope.$on('eventLogin', function (err,response) {
	 		$scope.users = response;
	 	});

		Refresh();

		$scope.submitLoginCredentials = function () {
			$loginService.submitLoginCredentials($scope.user);
			Refresh();
		};
	*/

}]);